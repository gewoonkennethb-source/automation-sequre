# Blokkeer alle directe toegang tot de database map
# Voorkomt dat de SQLite database via het web gedownload kan worden

<IfModule mod_authz_core.c>
  # Apache 2.4+
  Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
  # Apache 2.2
  Order deny,allow
  Deny from all
</IfModule>
